/**
 * Theme utility functions for managing dark mode state
 */

/**
 * Toggles the dark mode state
 * @param isDarkMode - Current dark mode state
 * @returns The toggled dark mode state
 */
export const toggleDarkMode = (isDarkMode: boolean): boolean => {
  return !isDarkMode;
};
